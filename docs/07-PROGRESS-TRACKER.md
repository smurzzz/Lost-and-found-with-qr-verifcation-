# ClaimIt — Progress Tracker

> Update status per task as work happens. Status legend: 🔲 Not started · 🟡 In progress · ✅ Done · 🔴 Blocked

## Milestone 1 — Foundations

| Task                                                  | Owner    | Status | Notes                                                                                  |
| ----------------------------------------------------- | -------- | ------ | -------------------------------------------------------------------------------------- |
| Repo scaffolded (Expo + TypeScript + Router)          | Codebuff | ✅     | Phase 0 done — project root on Expo SDK 57, strict TS, expo-router; committed          |
| Supabase project created, schema migrated             |          | 🔲     | wiring ready (`src/lib/supabase.ts`); needs real project URL/anon key in `.env` (0.4)  |
| Clerk project set up, domain allowlist configured     |          | 🔲     | wiring ready (`ClerkProvider` in `_layout.tsx`); needs publishable key in `.env` (0.4) |
| Design system tokens implemented (colors, typography) | Codebuff | ✅     | Phase 1 — mockup palette + Plus Jakarta Sans / DM Sans via `@expo-google-fonts`        |
| Navigation shell (student + staff route groups)       | Codebuff | ✅     | Phase 1 — `(student)` + `(staff)` groups, student/staff bottom navs, all 13 screens    |

## Phase 1 — UI Screens (all 13, static with mock data)

| Screen                | Flow    | Status | Notes                                                                                                                                   |
| --------------------- | ------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| 01 Login              | Student | ✅     | SSO button → student home; demo staff sign-in → dashboard                                                                               |
| 02 Student Home       | Student | ✅     | live search + category filters, local item photos, dismissal; redesigned to v3 mockup (navy/blue/orange, #EEF1F8 bg, compact Mine-card) |
| 03 Report Lost Item   | Student | ✅     | validated form, category/date sheets, image picker                                                                                      |
| 04 Report Found Item  | Student | ✅     | pending drop-off notice + confirmation state                                                                                            |
| 05 Possible Matches   | Student | ✅     | real match cards, dismiss, empty state                                                                                                  |
| 06 Claim Verification | Student | ✅     | passes tapped item, char counter, claim confirmation                                                                                    |
| 07 Staff Dashboard    | Staff   | ✅     | summary cards, 3 working tabs, staff cards, log-found CTA                                                                               |
| 08 Log Found Item     | Staff   | ✅     | validated intake form → generates CI-XXXXXX tag                                                                                         |
| 09 Confirm Receipt    | Staff   | ✅     | resolves tapped report/item, generates tag on confirm                                                                                   |
| 10 QR Tag Ready       | Staff   | ✅     | tag card + QR grid; mock print; shared `lib/qr-tag`                                                                                     |
| 11 Scan to Release    | Staff   | ✅     | animated viewfinder; camera simulated until Phase 6                                                                                     |
| 12 Audit Log          | Staff   | ✅     | lifecycle timeline with working stage filters                                                                                           |
| 13 Profile            | Both    | ✅     | shared `ProfileView`, per-role identity + nav, mock logout                                                                              |

## Milestone 2 — Auth & Roles (Phase 3)

| Task                                         | Owner | Status | Notes                                                                                                                                                                                                           |
| -------------------------------------------- | ----- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Student self-signup (SSO, domain-restricted) |       | 🟡     | Real Clerk SSO wired (`startSSOFlow` + `setActive`), `users_insert_self` policy (role forced to `student`), domain check in `sync-user.ts`. Awaiting Supabase JWT-issuer config + live run to mark done         |
| Staff invite-only provisioning flow          |       | 🟡     | `syncUserOnLogin` throws `StaffNotProvisionedError` for staff emails without a pre-existing row; provisioning is a service-role SQL insert of the staff row (never self-created) — SQL snippet in blocker notes |
| Role-based route guarding                    |       | ✅     | `RoleGuard` wraps `(student)` + `(staff)` layouts; session-aware `index.tsx`; wrong-role → correct home, logged-out → `/login`                                                                                  |
| Profile screen                               |       | ✅     | Student + staff profiles read the real session (name/initials/role via `useSession`), real `signOut()`; role-toggle previews are demo-mode only                                                                 |

## Milestone 3 — Staff Logging

| Task                          | Owner | Status | Notes                                                                                                                                                                                                   |
| ----------------------------- | ----- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Log Found Item form           |       | ✅     | Phase 4 — controlled + validated form (category chips, found-date stamp, photo placeholder); on submit posts to the `log-found` Edge Function; demo mode keeps the Phase 1 navigate/push click-through  |
| QR generation on submit       |       | ✅     | `supabase/functions/log-found` mints `FND-xxxxx` server-side + writes the `found` audit row; partial unique index migration `20250924000003_phase4_qr_unique.sql`; typed wrapper `src/lib/api/items.ts` |
| QR Tag Ready screen + print   |       | 🟡     | Real scannable QR via `react-native-qrcode-svg` from the server tag (routes: `/log-found → qr-tag?itemId=`); physical printing is still an OS-level task, not implemented                               |
| Found Items list (staff view) |       | 🟡     | Dashboard Found Items tab + first stat now read real rows (`useFoundItems` hook → `fetchItems`), `StaffItemCard` for DB items; demo mode falls back to mocks; awaiting live RLS pass                    |

## Milestone 4 — Student Reporting

| Task                              | Owner | Status | Notes                                                                                                                                                             |
| --------------------------------- | ----- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Report Lost Item form             |       | ✅     | Phase 5 — valid submit calls `insertLostReport` via `useCreateLostReport`; busy/error states on the shared `ReportForm`; demo mode still navigates only           |
| Report Found Item form            |       | ✅     | Phase 5 — `insertStudentFoundItem` (source `student_reported`, status `pending_dropoff`, `qr_code` NULL — CP-03). Item-name field added (items.title is NOT NULL) |
| "Pending drop-off" status + badge |       | 🟡     | Found-success screen shows the `dropoff` pill static; real-time status readback on a student's own report rides on the Phase 6 feed                               |
| My Lost Reports list              |       | ✅     | Student home section renders real rows via `LostReportCard` (lost-status→pill, incl. a new `StatusPill` label override); demo keeps the mock card                 |

## Milestone 5 — Shared Found Feed & Matching

| Task                                              | Owner | Status | Notes                                                                                                                                                      |
| ------------------------------------------------- | ----- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Unified scrollable Found Items feed               |       | ✅     | Phase 6 — home + Search tabs feed real `items` via `useFoundItems` → `Feed` `dbItems` mode; staff-logged + student-reported combined; demo fallback intact |
| Category filter + search                          |       | ✅     | filters live DB rows in `Feed` (title search, category chips)                                                                                              |
| Mine / Not mine actions on feed cards             |       | ✅     | `StudentItemCard`; Mine → `claim-verify?itemId=` resolves the real item; Not mine dismisses locally. The claim INSERT itself lands in Milestone 6          |
| Matching logic (category + keyword + date window) |       | ✅     | `find_possible_matches` RPC (migration 04) + `/match` Edge Function; category exact + 14-day window + keyword-overlap OR location proximity                |
| Push notification on probable match               |       | ✅     | `/match` sends Expo push to `users.push_token` (best-effort); client registers token via `usePushTokenSync` on sign-in                                     |
| Possible Matches screen                           |       | 🔲     | `matches.tsx` still mock — deferred (Phase 6 scope: matching + notify; screen surfaces real data next)                                                     |

## Milestone 6 — Claims

| Task                               | Owner | Status | Notes                                                                                                                                                                                                                                   |
| ---------------------------------- | ----- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Claim Verification screen          |       | ✅     | Phase 7 — Submit writes a real claim via `useClaimItem` → `insertClaim` (status `pending`, `claim_requested` audit); validates detail, busy/error states, demo nav intact                                                               |
| Claim submitted confirmation state |       | ✅     | `claim-success.tsx` reached after a successful insert (was mock nav; now real)                                                                                                                                                          |
| Staff Pending Claims tab           |       | ✅     | Phase 7 — real `pending` claims via `usePendingClaims` → `fetchPendingClaims` (joined claimant name + item details; RLS covers staff reads); loading/error/retry/empty + live stat count; demo keeps mock                               |
| Claim lifecycle server-side        |       | ✅     | Migration 05: trigger `available → pending_claim` on claim insert (excludes `claimed` and `pending_dropoff` — one-rule safe) + partial unique index = one pending claim per item. Approve/approval decisioning still lands with Phase 9 |

## Milestone 7 — Staff Confirm Receipt (student-reported items)

| Task                              | Owner | Status | Notes                                                                                                                                                                                     |
| --------------------------------- | ----- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Staff Student Reports tab         |       | ✅     | Phase 8 — real `pending_dropoff` items via `useStudentReports` (loading/error/empty + live stat), card → `confirm-receipt?itemId=`                                                        |
| Confirm Receipt screen            |       | ✅     | Phase 8 — resolves item by id, checkbox-gated, confirms via `/confirm-receipt` (staff-gated, precondition pending_dropoff once → 409 on repeats), routes to `qr-tag?itemId=`; demo intact |
| QR generated only on confirmation |       | ✅     | CP-04 — QR minted server-side via shared `_shared/claimit.ts` path (same as `/log-found`, 09 §9); client-side RLS `confirmReceipt` helper removed                                         |

## Milestone 8 — QR Release (core feature)

| Task                                               | Owner | Status | Notes                                                                                                                                |
| -------------------------------------------------- | ----- | ------ | ------------------------------------------------------------------------------------------------------------------------------------ |
| Camera scanning screen                             |       | ✅     | `scan.tsx` real expo-camera QR scan (permission/denial-recovery, invalid-tag + network error states) — Phase 9                       |
| Scan validation against item record                |       | ✅     | client resolves tag via `fetchItemByQrCode` (CP-05 invalid scans never reach the sheet); `/release` re-validates server-side         |
| Confirm Release bottom sheet                       |       | ✅     | real item + claims (`useItem`/`useClaimsForItem`), approve-via-RLS step, blocked without approved claim (CP-06), claimed state       |
| `/api/items/:id/release` endpoint, server-enforced |       | ✅     | `release` Edge Function rewired (itemId in body) + `releaseItem`/`useReleaseItem`; critical path — see TESTING-REPORT.md CP-01–CP-07 |
| Item Released success state                        |       | ✅     | `released.tsx` reflects real item title + claim id from the server response                                                          |

## Milestone 9 — Audit & Reporting

| Task                                         | Owner | Status | Notes                                                                                                                   |
| -------------------------------------------- | ----- | ------ | ----------------------------------------------------------------------------------------------------------------------- |
| Audit log list + status filters              |       | ✅     | `useAuditFeed` → `fetchAuditFeed` (real `audit_log` + item/actor joins); Phase 1 filter chips map to real item statuses |
| Expandable per-item timeline                 |       | ✅     | real chronological events per item (AL-02), actor + role or `System`, timestamp + note                                  |
| Audit entry auto-created on every transition |       | ✅     | every phase's writes already append audit rows (found/confirmed/matched/claim_requested/released) — see CP-07           |

## Milestone 10 — Polish & Submission

| Task                         | Owner | Status | Notes             |
| ---------------------------- | ----- | ------ | ----------------- |
| Full critical-path test pass |       | 🔲     | TESTING-REPORT.md |
| EAS Build (Android APK)      |       | 🔲     |                   |
| Demo rehearsal               |       | 🔲     | DEMO-GUIDE.md     |
| Documentation finalized      |       | 🔲     | this folder       |

## Blockers / open questions

| Date raised | Blocker                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Owner   | Resolution                                                                                                                                             |
| ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-09-24  | Real auth is code-complete but the live bridge needs two dashboard steps that can't be done from the repo: (1) **Supabase** → Auth → JWT Settings → add Clerk as a custom JWT issuer (JWKS URL) so `auth.uid()`/`auth.role()` resolve from the Clerk session JWT; (2) **Clerk** → JWT Templates → a `supabase` template whose `sub` is `{{user.id}}`. Until both exist, `applyClerkSupabaseToken` stores a token Supabase won't accept → sync errors surface on login.                                                                                                                                                                                                                              | (human) | Repeat steps in `mobile-jwt-issuer` note / Clerk docs; tick `05-TESTING-REPORT.md` AUTH-01                                                             |
| 2026-09-24  | Staff provisioning is invite-only by design (AGENTS one-rule-safe: staff rows are never self-created). To onboard a real staff tester, run a **service-role** insert: `insert into users (id, role, name, email) values ('<clerk_user_id>', 'staff', 'Maya Chen', 'mayac@school.edu') on conflict (id) do update set role = 'staff';` (Clerk id looks like `user_2…` — `users.id` is now `text`).                                                                                                                                                                                                                                                                                                   | (human) | Migrate `20250924000002_phase3_auth.sql` must be pushed first (`supabase db push`)                                                                     |
| 2026-09-24  | Phase 4 (staff logging) is code-complete but needs a live pass to close SL-01: deploy the new `log-found` Edge Function (`supabase functions deploy log-found`, service-role key already required), push migration `20250924000003_phase4_qr_unique.sql`, then log a real item from the staff device and confirm the QR Tag screen shows a scannable QR that survives the Verify-Item readback. Underlying production-hardening TODO (not blocking): both Edge Functions decode the Clerk JWT payload without verifying the signature — acceptable for the current self-hosted/demo deployment, but swap to real signature verification (or Supabase's JWT-gateway path) before any public rollout. | (human) | `supabase db push` + `supabase functions deploy log-found`; run `05-TESTING-REPORT.md` SL-01; note in milestone 8 CP-05 when hardening the token check |
| 2026-09-24  | Phase 6 (shared feed + matching) is code-complete but its exit criterion needs a live device: push migration `20250924000004_phase6_matching.sql`, deploy `/match` (`supabase functions deploy match`), run `eas init` so `expo-constants.easConfig.projectId` exists (without it Expo Go can't mint a push token — token registration then no-ops and matching still works in-app), sign in as Alex on a real device so `users.push_token` is saved, then call `/match` for the seeded report `20000000-0000-4000-8000-000000000001` — it must match the White headphones item (Electronics + keyword overlap) and deliver a real notification (M-01).                                             | (human) | `supabase db push` + `supabase functions deploy match` + `eas init`; run M-01 on-device                                                                |

## Weekly log

| Week of    | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Next steps                                                                                                                                             |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-09-22 | Phase 0 (0.1–0.3, 0.5–0.7) complete on Expo SDK 57: tooling verified, project root scaffolded, deps installed, ESLint/Prettier/husky set up, `.env`/`.env.example`/`app.config.ts` wired, expo-doctor 21/21, Android bundle exports, dev server verified, committed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 0.4: create Supabase + Clerk projects with real keys → fill `.env`; set up EAS project (`eas init`); push repo to remote; then Phase 1                 |
| 2026-09-23 | Phase 1 (all 13 UI screens) complete with mock data: design system (tokens, fonts, shared components), student flow (login → home → report lost/found → matches → claim), staff flow (dashboard → log found → confirm receipt → QR tag → scan → audit), shared profile; every screen tsc/ESLint-clean and interaction-verified live; committed per screen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Phase 2+ : real auth (0.4 keys), data layer, camera scanning, matching engine per 08-PHASE-PLAN.md                                                     |
| 2026-09-24 | Phase 3 real auth (code side) landed on top of the v3 redesign: real Clerk SSO (`startSSOFlow` + `setActive`), Clerk→Supabase token bridge (`authBridge` applies the Clerk session JWT via `supabase.auth.setSession`), users-row sync with `users_insert_self` policy, `RoleGuard` on both route groups, session-aware entry route, profiles + home greeting read `useSession` (initials helper, real logout), staff demo toggle demo-only. Migration `20250924000002_phase3_auth.sql` converts `users.id` + FK columns `uuid → text` (real Clerk ids are `user_…`), rewrites RLS comparisons to `auth.uid()::text`, adds `users_insert_self`. `npm run typecheck` + `npm run lint` clean. Needs the two dashboard config steps (blockers above) + a live device pass to close AUTH-01/02.                                                                                                                                                                                                                                                                                                                                                        | Apply migration + dashboard config, provision `mayac@school.edu`, run AUTH-01/02, then Phase 4 data-layer screens                                      |
| 2026-09-24 | Phase 4 staff logging (wire the real thing) landed: `log-found` Edge Function (staff-only, server-minted `FND-xxxxx` QR + `found` audit row), migration `20250924000003_phase4_qr_unique.sql` (partial unique index on `items.qr_code`), `react-native-qrcode-svg` dependency, typed `/lib/api/items.ts` + `/lib/hooks/use-items.ts` (React Query), controlled+validated Log Found form → `qr-tag?itemId=`, real scannable QR on the QR Tag screen, and a DB-driven Found Items list with demo-mode fallback. `npm run typecheck` + `npm run lint` clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Deploy `log-found` function + push migration 03; run SL-01 live; then Phase 5 (student reporting)                                                      |
| 2026-09-24 | Phase 5 student reporting (wire the real thing) landed: report-lost → `lost_reports` and report-found → `items` (`pending_dropoff`, `qr_code` NULL — CP-03) through new `/lib/hooks/use-reports.ts` mutations; the shared `ReportForm` gained an item-name field (Found only, `items.title` NOT NULL), busy/error states, and a typed `onSubmit(values)`; student home "My Lost Reports" now fetches real rows (`useMyLostReports` → `LostReportCard` with a `StatusPill` label override + lost-status mapping), demo fallback intact. `npm run typecheck` + `npm run lint` + prettier clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Live-run SR-01/02/03; then Phase 6 (shared feed + matching)                                                                                            |
| 2026-09-24 | Phase 6 shared feed + matching (wire the real thing) landed: student home + Search feed real combined items via `useFoundItems` → `Feed` `dbItems` mode (search + category on live rows, `claimed` hidden, Mine → `claim-verify?itemId=` via `useItem`, Not mine dismisses locally — `StudentItemCard`); server-side matching `find_possible_matches` RPC (migration `20250924000004_phase6_matching.sql`: same category, 14-day window, keyword-overlap OR location) driven by the new `/match` Edge Function (owner/staff-gated) which flips `lost_reports` → `possible_match` once, writes `matched` audit rows, and best-effort Expo-pushes to `users.push_token`; client push-token registration (`src/lib/push.ts` + `usePushTokenSync`, RLS-enforced via column grant) and `/match` auto-triggered after filing a lost report. `npm run typecheck` + `npm run lint` clean.                                                                                                                                                                                                                                                                  | Live-run M-01 exit criterion (migration 04, deploy `match`, `eas init`, push token, call `/match` for seeded report); then Phase 7 (claims)            |
| 2026-09-24 | Phase 8 confirm receipt (wire the real thing) landed: staff Student Reports tab reads real `pending_dropoff` items (`useStudentReports` → `fetchStudentReports`, loading/error/empty + live stat), each card → `confirm-receipt?itemId=`; Confirm Receipt resolves the item via `useItem`, requires the details-match checkbox, and confirms through the new `/confirm-receipt` Edge Function — staff-gated (shared `requireStaffUser`), preconditioned on `source = 'student_reported'` + `status = 'pending_dropoff'` (exactly-once; 409 on repeats/foreign items), mints the server-side `FND-xxxxx` QR (shared `uniqueQrCode` — new `supabase/functions/_shared/claimit.ts`, `log-found` refactored to import it per 09 §9), writes the `confirmed` audit row, then routes to `qr-tag?itemId=` with the real scannable QR (CP-04). Client-side RLS `confirmReceipt` helper removed (dead + would mint on-device). No migration needed. `npm run typecheck` + `npm run lint` clean.                                                                                                                                                             | Deploy `supabase functions deploy` (bundles `_shared`; re-deploys `log-found`); run CR-01/02/03 + CP-04 live; then Phase 9 (QR Release — core feature) |
| 2026-09-24 | Phase 9 QR Release (the core feature, wire the real thing) landed: `scan.tsx` does real QR scanning via expo-camera `CameraView` (permission + denied-recovery, invalid-tag + retry states, resolves the tag with `fetchItemByQrCode` so a bogus scan never reaches the sheet — CP-05); the `release` Edge Function was rewired — `itemId` now travels in the POST body, since Supabase invokes functions at `/functions/v1/release` and `/items/:id/release` could never match from the native client — keeping §4 verbatim (staff auth → scanned QR matches item → claim `approved` else 409 `claim_not_approved` (CP-06) → `released` audit row → `items.status='claimed'` strictly last); `releaseItem`/`useReleaseItem` drive Confirm Release; `release.tsx` is now a real sheet (`useItem` + `useClaimsForItem`, claimant + verification answer, Approve Claim via RLS `useApproveClaim`, blocked no-approved-claim + already-claimed states) and routes to a `released.tsx` success screen that reflects the real item title + claim id. No migration needed (`expo-camera` already installed). `npm run typecheck` + `npm run lint` clean. | Deploy the `release` function / run QR-01/02/03 + CP-05/06/07 live on device; then Phase 10 (Audit & Reporting)                                        |
| 2026-09-24 | Phase 10 Audit & Reporting (wire the real thing) landed: `useAuditFeed` → `fetchAuditFeed` now reads the append-only `audit_log` table with item + actor joins (`audit_log_item_id_fkey` / `audit_log_actor_id_fkey`; RLS `audit_log_select` lets any authenticated user read — no Edge Function needed) and groups events per item (chronological within, newest item first). The staff `audit.tsx` real mode keeps the Phase 1 filter chips (All / Unclaimed / Pending Claim / Claimed — now mapped to real item statuses) and expandable timeline visuals, but each step renders the real event label, actor name + role (or `System`), timestamp, and the event note (e.g. the released handoff's claim id); loading / error+retry / empty states are explicit and demo mode keeps the mocks. No migration needed. `npm run typecheck` + `npm run lint` clean.                                                                                                                                                                                                                                                                                 | Live-run AL-01/02/03 (needs a settled lifecycle on a device first); then Phase 11 (Polish, Test, Submit)                                               |
