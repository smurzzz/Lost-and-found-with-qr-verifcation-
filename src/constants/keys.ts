/** Keys used with expo-secure-store. Keep names stable across phases. */
export const CLERK_TOKEN_KEY = 'claimit.clerk.supabase-token';

/** Notification permission / push token storage keys (used from Milestone 5). */
export const EXPO_PUSH_TOKEN_KEY = 'claimit.notifications.expo-push-token';
