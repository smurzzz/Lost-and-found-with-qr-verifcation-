import PlaceholderScreen from '@/components/placeholder-screen';

/** Phase 1 stub — full Report Lost Item mockup arrives next (09 §3). */
export default function ReportLostScreen() {
  return (
    <PlaceholderScreen
      title="Report Lost Item"
      note="Stub — the full mockup implementation is coming in this Phase 1 series."
    />
  );
}
