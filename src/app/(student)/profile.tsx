import PlaceholderScreen from '@/components/placeholder-screen';

/** Phase 1 stub — full Profile mockup arrives later in this series (09 §13). */
export default function ProfileScreen() {
  return (
    <PlaceholderScreen
      title="Profile"
      note="Stub — the full mockup implementation is coming in this Phase 1 series."
    />
  );
}
