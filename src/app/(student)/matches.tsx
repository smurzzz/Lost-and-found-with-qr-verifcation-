import PlaceholderScreen from '@/components/placeholder-screen';

/** Phase 1 stub — full Possible Matches mockup arrives next (09 §5). */
export default function MatchesScreen() {
  return (
    <PlaceholderScreen
      title="Possible Matches"
      note="Stub — the full mockup implementation is coming in this Phase 1 series."
    />
  );
}
