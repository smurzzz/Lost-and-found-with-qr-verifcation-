import PlaceholderScreen from '@/components/placeholder-screen';

/** Phase 1 stub — full Claim Verification mockup arrives next (09 §6). */
export default function ClaimVerifyScreen() {
  return (
    <PlaceholderScreen
      title="Claim Verification"
      note="Stub — the full mockup implementation is coming in this Phase 1 series."
    />
  );
}
