import PlaceholderScreen from '@/components/placeholder-screen';

export default function StaffAuditScreen() {
  return (
    <PlaceholderScreen title="Audit Log" note="Full audit trail (screen 12) — mockup pending." />
  );
}
