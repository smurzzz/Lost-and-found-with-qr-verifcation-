import PlaceholderScreen from '@/components/placeholder-screen';

export default function StaffQrTagScreen() {
  return (
    <PlaceholderScreen title="QR Tag" note="QR tag print/display (screen 10) — mockup pending." />
  );
}
