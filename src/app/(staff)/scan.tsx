import PlaceholderScreen from '@/components/placeholder-screen';

export default function StaffScanScreen() {
  return <PlaceholderScreen title="Scan QR" note="QR camera flow (screen 11) — mockup pending." />;
}
