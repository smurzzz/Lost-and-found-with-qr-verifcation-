import PlaceholderScreen from '@/components/placeholder-screen';

export default function StaffProfileScreen() {
  return (
    <PlaceholderScreen
      title="Staff Profile"
      note="Shared profile screen (13) — staff variant pending."
    />
  );
}
