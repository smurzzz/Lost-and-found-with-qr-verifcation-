import PlaceholderScreen from '@/components/placeholder-screen';

export default function StaffConfirmReceiptScreen() {
  return (
    <PlaceholderScreen
      title="Confirm Receipt"
      note="Drop-off confirmation flow (screen 09) — mockup pending."
    />
  );
}
